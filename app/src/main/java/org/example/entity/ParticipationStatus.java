package org.example.entity;

public enum ParticipationStatus {
    ACTIVE,     // User is actively participating
    INACTIVE,   // User is not currently active
    SUSPENDED,  // User participation is suspended
    COMPLETED   // User has completed their role in the case
} 