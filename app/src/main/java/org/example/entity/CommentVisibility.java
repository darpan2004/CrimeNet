package org.example.entity;

public enum CommentVisibility {
    PUBLIC,     // Visible to all case participants
    PRIVATE,    // Visible only to the comment author
    TEAM        // Visible to team members only
} 