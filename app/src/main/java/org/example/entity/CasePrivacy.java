package org.example.entity;

public enum CasePrivacy {
    PUBLIC,     // Visible to all users
    PRIVATE,    // Only visible to invited users
    RESTRICTED  // Visible to organization members only
} 